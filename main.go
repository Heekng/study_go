package main

import "fmt"

func main() {
	//상수
	//const name string = "heekng"
	//fmt.Println(name)

	//var name string = "heekng"
	// 이렇게 작성하면 var, type을 자동 설정해준다.
	// 축약형은 func 안에서만 사용 가능
	name := "heekng"
	name = "heekng2"
	fmt.Println(name)
}
